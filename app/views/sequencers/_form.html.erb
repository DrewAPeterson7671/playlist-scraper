<%= form_with(model: sequencer, local: true) do |form| %>
  <% if sequencer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sequencer.errors.count, "error") %> prohibited this sequencer from being saved:</h2>

      <ul>
      <% sequencer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sequencer_name %>
    <%= form.text_field :sequencer_name %>
  </div>

  <div class="field">
    <%= form.label :draft_id %>
    <%= form.text_field :draft_id %>
  </div>

  <div class="field">
    <%= form.label :sequence_position %>
    <%= form.text_field :sequence_position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
